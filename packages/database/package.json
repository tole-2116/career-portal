{
  "name": "@career-portal/database",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:migrate:dev": "prisma migrate dev",
    "db:migrate:prod": "prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "db:seed": "bun run prisma/seed.ts",
    "db:seed:prod": "NODE_ENV=production bun run prisma/seed.ts",
    "db:seed:dev": "NODE_ENV=development bun run prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "bcryptjs": "^3.0.3"
  },
  "devDependencies": {
    "@career-portal/typescript-config": "workspace:*",
    "@types/bcryptjs": "^2.4.6",
    "prisma": "^5.18.0",
    "typescript": "^5.8.3"
  },
  "prisma": {
    "seed": "bun run prisma/seed.ts"
  }
}
